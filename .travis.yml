language: cpp

sudo: enabled

compiler:
    - g++

os:
    - linux

addons:
    apt:
        - libpcap-dev
        - libssl-dev

before-script:
    - sudo chmod +x setup.sh
    - sudo ./setup.sh
    - make
    - cd source/tests
    - make check

scripts:
    - ./build/snicker
